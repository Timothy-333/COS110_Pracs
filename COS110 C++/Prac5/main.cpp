#include <iostream>
#include <TesterInterface.h>
#include <ValueIndependantTester.h>
#include <IsPrimeNumber.h>

using namespace std;

int main() 
{
    // TesterInterface tester = new TesterInterface(5);
    // tester.addTester(new IsPrimeNumber());
    return 0;
}